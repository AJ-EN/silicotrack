# SilicoTrack — local development
#
# Prototype datastore is SQLite. The production path is Postgres on the
# Rajasthan State Data Centre; switching means changing this URL, the
# datasource provider in prisma/schema.prisma, and the driver adapter in
# src/lib/db/client.ts. Nothing else.
DATABASE_URL="file:./dev.db"
